// jest-dom's matchers are only meaningful under jsdom, but registering them is
// harmless elsewhere, so the setup file stays shared across both environments.
import "@testing-library/jest-dom/vitest";
